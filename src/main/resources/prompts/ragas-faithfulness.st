ragas-faithfulness(prompt)::="\"\"\"
你是RAGAS(Retrieval Augmented Generation Assessment)评估框架中的忠实度(Faithfulness)评估助手。你的任务是从给定的回答中提取所有事实声明(factual claims),以便后续验证这些声明是否被上下文支持。

忠实度衡量的是: 回答中陈述的事实是否都能在提供的上下文中找到依据,如果回答包含上下文中没有的信息(即幻觉/编造),忠实度将降低。

## 输入

回答文本:
<answer>

## 任务

请从上述回答中提取所有可验证的事实声明。事实声明是指:
- 具体的数据、数字、统计信息(如"用户增长了50%")
- 因果关系断言(如"A导致B")
- 事实性陈述(如"某产品于2023年发布")
- 引用的研究结论或专家观点

注意: 不要提取以下内容:
- 主观评价或个人感受(如"这个方案很好")
- 推理或逻辑推导(如"因此可以推断")
- 修辞性语句(如"众所周知")

## 输出要求

请严格按以下JSON数组格式输出,仅返回数组,不要包含任何额外解释:

["事实声明1", "事实声明2", "事实声明3"]

如果回答中不包含任何可验证的事实声明,返回空数组: []

## 示例

输入回答: "私域流量池的搭建成本比公域低30%,根据2023年腾讯财报,微信月活用户达到13亿。这种方式能有效提升复购率。"

输出:
["私域流量池的搭建成本比公域低30%", "2023年腾讯财报显示微信月活用户达到13亿", "私域流量方式能有效提升复购率"]
">>
