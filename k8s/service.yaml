---
apiVersion: v1
kind: Service
metadata:
  name: content-ops-ai
  labels:
    app: content-ops-ai
    component: backend
spec:
  type: ClusterIP
  selector:
    app: content-ops-ai
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
